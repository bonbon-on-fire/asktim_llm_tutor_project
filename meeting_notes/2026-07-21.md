# Meeting Notes: 07/21/2026

## Attendees

- Faizan Siddiqi
- Laura Lara
- Matt Toy
- Romain Puech
- Nishita Bhakar

## Agenda

- Demo the **AskTIM Database** review dashboard's new per-message metadata and
  get feedback on whether it's enough to review/debug tutor behavior at scale:
  - Each tutor turn now shows the **model used + estimated cost** (e.g.
    `gpt-5.4 ($0.0338)`), a collapsible **RAG retrieval** list of the chunks
    pulled that turn (score / source / full text), and the student's
    **thumbs up/down** rating (display-only — the read-only dashboard shows the
    stored rating but never overwrites student feedback) — mirroring what the
    Sandbox surfaces.
- Discuss **cost visibility** now that it's exposed: each conversation shows a
  **running total cost** in the sidebar. Decide whether we want cost rollups /
  budgets / alerting (e.g. per student, per course, or flagging high-cost turns).
- Confirm the **sidebar/review UX**: entries now lead with the student's
  **username** (in crimson, for anonymous rows too), then the **full course
  name** (e.g. `MIT CTL.SC2x Supply Chain Design`), then the exercise header
  with total cost. Header reworded to "AskTIM · Database Beta+".
- Decide what else reviewers need from the dashboard — e.g. filter/sort by
  rating, surface thumbs-down turns, or aggregate cost — and whether the
  Database dashboard is the right home for TA review vs. the disk-based
  `dashboard_ui`.
- Open question: should thumb ratings stay display-only, or do we want reviewers
  to be able to tag / triage conversations from here?

## Notes

## Decisions

## Action items

## Resources
